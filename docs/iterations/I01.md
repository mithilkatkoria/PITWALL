# Iteration 1: core models and validation

Objective: FR01..FR04. Design and pseudocode: `docs/design/architecture.md`. Files: `src/pitwall/models.py`, `tests/conftest.py`, `tests/test_models.py`, `scripts/run_tests.py`.

Implementation uses frozen dataclasses, a compound enum, finite numeric checks, immutable tuple collections and race-dependent strategy validation. `Strategy.validate_for` checks the final-lap constraint; constructor validation rejects duplicates and unordered stops rather than silently sorting them. This separates local object validity from race context.

Tests: TEST-I01 executed 2026-09-08 UTC. Expected: valid inputs accepted and erroneous inputs raise ValueError. Actual: 20 passed. Raw output, JUnit XML and source hashes are in `evidence/test-runs/TEST-I01-20260908T205847968425Z/`.

Genuine test failures: none observed. Diagnosis, remedial action and retest: N/A. Environment issue: dependency installation failed at a PySide6 long pathname; a shorter runtime path is being prepared. This is an installation problem, not a manufactured code-test failure. Original tool output remains in the task conversation.

Screenshot evidence: pending, no GUI yet. [GENUINE STAKEHOLDER FEEDBACK PENDING]

Review: boundary validation passed; the cross-object final-lap rule required an explicit convention. An alternative mutable model would simplify edits but permit invalid states after construction. Lesson: validate newly constructed inputs at the GUI boundary. Implementation matches the recorded class design. Candidate reflection and explanation are pending; these notes are a factual assistant review. Next: tyre degradation.
