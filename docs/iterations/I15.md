# Iteration 15: scenario persistence

FR09/A14 design `docs/design/I15-persistence.md`. Added strict version1 schema and atomic-save module. TEST-I15:7 tests passed, evidence `evidence/test-runs/TEST-I15-20260908T211824033057Z/`. Exact event/seed/stop-time round-trip passed; malformed JSON, duplicate keys, unsupported versions, invalid length and oversized files rejected. No observed failure; remedy/retest N/A. Screenshot pending GUI. [GENUINE STAKEHOLDER FEEDBACK PENDING]

Review: validating before replacement prevents invalid saved scenarios and temporary files are cleaned. Atomic replacement depends on filesystem semantics. Search/run preferences are separate from race data. Chart iteration14 is intentionally combined with16 as recorded before implementation. Candidate reflection pending.
